import SwiftUI

struct ContentView: View {
    @State var ans:Int = 0
    var body: some View {
        VStack {
            Button {
                <#code#>
            } label: {
                Text("Multiply")
            }
            Text("\(ans)")
        }
    }
}
